type Mutation{
    novoUsuario(
        dados: UsuarioInput!
        ): Usuario!

    excluirUsuario(id : Int!): Usuario

    editarUsuario(
        nome: String
        email: String
        idade: Int
        id: Int!
    ): Usuario
}