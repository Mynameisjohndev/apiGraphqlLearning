type Mutation{
    novoUsuario(
        dados: UsuarioInput!
        ): Usuario!

    excluirUsuario(
        filtro: UsuarioFiltro!
    ): Usuario

    editarUsuario(
        nome: String
        email: String
        idade: Int
        id: Int!
    ): Usuario

    
}