type Mutation{
    novoUsuario(
        nome: String
        email: String
        idade: Int
        ): Usuario!

    excluirUsuario(id : Int!): Usuario

    editarUsuario(
        nome: String
        email: String
        idade: Int
        id: Int!
    ): Usuario
}